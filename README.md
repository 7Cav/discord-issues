# discord-issues
Simple bot to create / manage github issues

## Setup / Local dev

Setup your `.env` file then populate the values:

```shell script
cp .env.example .env
```

Run via:

```shell script
npm install
npm start
```
